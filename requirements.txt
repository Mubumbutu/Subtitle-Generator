# WhisperX Subtitle Generator - Requirements

# WhisperX
whisperx

# Pin transformers to <5.0 - transformers 5.x requires torch>=2.6 for torch.load
# which is incompatible with the currently shipped torch 2.5.x CUDA builds
transformers>=4.48.0,<5.0.0

# Audio processing
sounddevice
soundfile
numpy

# Subtitles
pysrt

# GUI
PyQt6

# Audio resampling (optional but recommended)
librosa

# Voice separation
demucs
